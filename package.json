{
  "name": "TiltQuest",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "build": "vite build",
    "deploy": "vite build && gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "ammo": "^3.0.3",
    "three": "^0.170.0",
    "three-to-ammo": "^1.0.1"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "vite": "^6.0.6"
  }
}
